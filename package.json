{
  "name": "range-list",
  "version": "0.0.1",
  "description": "Range List including list of ranges defined as [number, number)",
  "main": "lib/RangeList.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "build": "babel src/ -d lib/",
    "prepublish": "yarn run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "rl"
  ],
  "author": "Jayden Li",
  "license": "ISC",
  "devDependencies": {
    "@babel/cli": "^7.14.5",
    "@babel/core": "^7.14.6",
    "@babel/preset-flow": "^7.14.5",
    "flow-bin": "^0.155.0",
    "jest": "^27.0.6"
  }
}
