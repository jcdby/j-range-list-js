{
  "name": "j-range-list-js",
  "version": "0.0.4",
  "description": "Range List including list of ranges defined as [number, number)",
  "main": "lib/RangeList.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "build": "babel src/ -d lib/",
    "test": "jest --coverage",
    "patchPublish": "yarn build && yarn version --patch -m 'Upgrade to %s' && npm publish"
  },
  "keywords": [
    "rl"
  ],
  "author": "Jayden Li",
  "license": "ISC",
  "devDependencies": {
    "@babel/cli": "^7.14.5",
    "@babel/core": "^7.14.6",
    "@babel/plugin-transform-modules-commonjs": "^7.14.5",
    "@babel/preset-env": "^7.14.7",
    "@babel/preset-flow": "^7.14.5",
    "babel-preset-env": "^1.7.0",
    "babel-preset-minify": "^0.5.1",
    "flow-bin": "^0.155.0",
    "jest": "^27.0.6"
  }
}
